---
"astro-adapter-oester": minor
---

First release on npm. The adapter used to be installed from a local copy as `@oester/astro`; install `astro-adapter-oester` instead and change the import in `astro.config`.
